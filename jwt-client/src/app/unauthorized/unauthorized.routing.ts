import { Routes } from '@angular/router';
import { UnauthorizedComponent } from './unauthorized.component';

export const unauthorizedRoute : Routes = [
    {path:'',component:UnauthorizedComponent}
]